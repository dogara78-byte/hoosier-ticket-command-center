# Patch 10 - Member Views + Settlement Polish

Adds member-facing balance cards, live seat ownership totals, parking-member totals, and a settlement report built from the live TransactionsTable after OneDrive sign-in.

Upload/overwrite index.html, styles.css, app.js, graph-client.js, ledger-fallback.json, and README_PATCH_10.md. Keep your existing config.js.

Open with ?v=patch10member to bypass cache.

Note: non-manager viewers can use the static fallback dashboard without OneDrive only if the data is included in the public repo. Live OneDrive workbook access requires Microsoft sign-in and permission to the workbook, unless a separate backend/proxy or published snapshot flow is added.
