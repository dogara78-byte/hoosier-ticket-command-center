# Patch 6 - Real TransactionsTable writeback

Upload these files to the GitHub repo root and keep your existing config.js:

- index.html
- styles.css
- app.js
- graph-client.js
- data/ledger-fallback.json

This patch enables the Manager screen to append rows to TransactionsTable using Microsoft Graph.
Use a 0.01 test row first, then verify it appears in the workbook and delete it from Excel.
