# Patch 20B - Workbook Status Render Fix

Fixes a public/member render error where the Score page called `workbookStatus()` before the helper existed.

Version: v2026.06.25-patch20b-workbook-status-fix

Upload the package files and keep your existing config.js.
